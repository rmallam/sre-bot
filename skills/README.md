# Optional team runbooks for CI/CD and deploy context.
# Files here are loaded into brain prompts via CICD_SKILLS_DIR.

Place `*.md` files in this directory (e.g. `ci-rerun-policy.md`, `deploy-checklist.md`).

**Auto-compiled remediation skills:** After incidents are remediated, use **Export skills** in the Operations Console (Resources page) to copy markdown snippets from successful/failed attempts. Paste edited snippets here so the brain can reuse proven fixes.
