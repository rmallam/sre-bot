apiVersion: apps/v1
kind: Deployment
metadata:
  name: fixture-liveness-probe-fail
  namespace: sre-fixture-lab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fixture-liveness-probe-fail
  template:
    metadata:
      labels:
        app: fixture-liveness-probe-fail
    spec:
      containers:
        - name: app
          image: busybox:1.36
          command: ["sleep", "3600"]
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
